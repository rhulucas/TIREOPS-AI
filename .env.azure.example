# Azure / shared environment example
# Use this as a template for staging or production.
# Keep real credentials in your deployment platform, not in git.

DATABASE_URL="postgresql://username:password@your-server.postgres.database.azure.com:5432/postgres?sslmode=require"
AUTH_SECRET="replace-with-a-long-random-secret"
OPENAI_API_KEY=""
